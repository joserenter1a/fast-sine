System design shift, I have the proof of concept, ability to sign docs. I want this to be a centralized contract management application, but lightweight for those signing (no account management for clients, only contractors to manage).

A contractor creates a new contract entry for a client, adds in the client name, contact info and details, etc. Is able to send that contract entry as a link to their client for them to just be able to sign (assigned by some unique token or identifier) they sign it and render the fully signed PDF using the client interface. This updates the management
